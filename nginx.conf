server {
    listen 80;
    server_name 46.101.117.103; 

    location = /favicon.ico { access_log off; log_not_found off; }

    location /static/ {
        alias /code/staticfiles/;
    }

    location / {
        include proxy_params;
        proxy_pass http://app_server;
    }
}